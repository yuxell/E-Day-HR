@using KD25_BitirmeProjesi.UI.MVC_Core.Areas.Personel.Models.Enums
@* @model IEnumerable<KD25_BitirmeProjesi.UI.MVC_Core.Areas.Personel.Models.DTOs.LeaveRecord_DTOs.ListLeaveRecord_DTO>

@{
    ViewData["Title"] = "ListLeaveRecord";
}

<h1>ListLeaveRecord</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ApprovalDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TotalDays)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StartDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EndDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LeaveRecordType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ApprovalStatus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ResponseDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AppUser)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ApprovalDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TotalDays)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.StartDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.EndDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LeaveRecordType)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ApprovalStatus)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ResponseDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.AppUser)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                </td>
            </tr>
        }
    </tbody>
</table>
 *@


@*  @model IEnumerable<KD25_BitirmeProjesi.UI.MVC_Core.Areas.Personel.Models.ViewModels.LeaveRecord_VMs.ListLeaveRecord_VM>

@{
    ViewData["Title"] = "İzin Kayıtları";
}

<h2>İzin Kayıtları</h2>

<div class="mb-3 text-end">
    <a asp-action="AddLeaveRecord" class="btn btn-primary btn-sm">Yeni İzin Ekle</a>
</div>

<table class="table table-bordered table-hover">
    <thead class="table-light">
        <tr>
            <th>ID</th>
            <th>Onay Tarihi</th>
            <th>Toplam Gün</th>
            <th>Başlangıç</th>
            <th>Bitiş</th>
            <th>İzin Türü</th>
            <th>Durum</th>
            <th>Cevap Tarihi</th>
            <th>Kullanıcı</th>
            <th>İşlemler</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.ID</td>
                <td>@item.ApprovalDate</td>
                <td>@item.TotalDays</td>
                <td>@item.StartDate</td>
                <td>@item.EndDate</td>
                <td>@item.LeaveRecordType</td>
                <td>
                    <span class="badge bg-@(
                        item.ApprovalStatus == ApprovalStatus.Approved ? "success" :
                        item.ApprovalStatus == ApprovalStatus.Cancelled ? "danger" : "warning"
                    )">
                        @item.ApprovalStatus.ToString()
                    </span>
                </td>
                <td>@item.ResponseDate</td>
                <td>@item.AppUser</td>
                <td>
                    <a asp-action="GetLeaveRecordDetails" asp-route-id="@item.ID" class="btn btn-info btn-sm">Detay</a>
                </td>
                <td>
                    @Html.ActionLink("UpdateLeaveRecord", "PersonelLeaveRecord", new { id=item.ID }) |
                    @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                </td>
            </tr>
        }
    </tbody>
</table> *@


@model IEnumerable<KD25_BitirmeProjesi.UI.MVC_Core.Areas.Personel.Models.ViewModels.LeaveRecord_VMs.ListLeaveRecord_VM>

@{
    ViewData["Title"] = "İzin Taleplerim";
}

@if (Model != null && Model.Any())
{
    <div class="col-lg-100 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <h4 class="card-title">İzin Talepleri</h4>
                    <a asp-action="AddLeaveRecord" class="btn btn-primary btn-sm">
                        <i class="mdi mdi-plus-circle"></i> Yeni İzin Ekle
                    </a>
                </div>
                <p class="card-description">
                    İzin talebi işlemleri
                </p>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>İzin Türü</th>
                                <th>Başlangıç Tarihi</th>
                                <th>Bitiş Tarihi</th>
                                <th>Toplam Gün</th>
                                <th>Durum</th>
                                <th>İşlem</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr data-id="@item.ID">
                                    <td>@item.LeaveRecordType</td>
                                    <td>@item.StartDate.ToString("dd.MM.yyyy")</td>
                                    <td>@item.EndDate.ToString("dd.MM.yyyy")</td>
                                    <td>@item.TotalDays</td>
                                    <td>
                                        @if (item.ApprovalStatus == ApprovalStatus.Approved)
                                        {
                                            <label class="badge badge-success">@item.ApprovalStatus</label>
                                        }
                                        else if (item.ApprovalStatus == ApprovalStatus.Cancelled)
                                        {
                                            <label class="badge badge-info">@item.ApprovalStatus</label>
                                        }
                                        else if (item.ApprovalStatus == ApprovalStatus.Denied)
                                        {
                                            <label class="badge badge-danger">@item.ApprovalStatus</label>
                                        }
                                        else
                                        {
                                            <label class="badge badge-warning">@item.ApprovalStatus</label>
                                        }
                                    </td>
                                    <td>
                                        <a asp-action="GetLeaveRecordDetails" asp-route-id="@item.ID"
                                           class="btn btn-sm btn-info" title="Detaylar">
                                            <i class="mdi mdi-eye"></i>
                                        </a>
                                        @if (item.ApprovalStatus == ApprovalStatus.Pending)
                                        {
                                            <a asp-action="UpdateLeaveRecord" asp-route-id="@item.ID"
                                               class="btn btn-sm btn-warning" title="Düzenle">
                                                <i class="mdi mdi-pencil"></i>
                                            </a>
                                            <button type="button" class="btn btn-sm btn-danger cancel-btn"
                                                    data-id="@item.ID" title="İptal Et">
                                                <i class="mdi mdi-delete"></i>
                                            </button>
                                        }
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
}
else
{
    <div class="alert alert-info">
        <i class="mdi mdi-information-outline"></i> Henüz izin talebi bulunmamaktadır.
    </div>
}

<!-- Toast Mesaj Kutusu -->
<div id="toast" style="position: fixed; bottom: 20px; right: 20px; display: none; padding: 15px; background: green; color: white; border-radius: 5px; z-index: 9999;"></div>

<script>
    document.addEventListener("DOMContentLoaded", function () {
        // İzin iptal butonları
        document.querySelectorAll(".cancel-btn").forEach(button => {
            button.addEventListener("click", function () {
                const id = this.getAttribute("data-id");
                const row = this.closest("tr");

                if (confirm("Bu izin talebini iptal etmek istediğinize emin misiniz?")) {
                    fetch(/api/Leave/Cancel/${id}, {
                        method: "PUT"
                    })
                    .then(response => {
                        if (response.ok) {
                            // Satırı yavaşça kaldır
                            row.style.transition = "opacity 0.5s ease";
                            row.style.opacity = 0;
                            setTimeout(() => row.remove(), 500);

                            // Başarı toast'ını göster
                            showToast("İzin talebi başarıyla iptal edildi.", true);
                        } else {
                            response.text().then(msg => showToast(msg, false));
                        }
                    })
                    .catch(error => {
                        showToast("Bir hata oluştu: " + error.message, false);
                    });
                }
            });
        });

        // Toast mesaj fonksiyonu
        function showToast(message, isSuccess) {
            const toast = document.getElementById("toast");
            toast.textContent = message;
            toast.style.backgroundColor = isSuccess ? "#28a745" : "#dc3545";
            toast.style.display = "block";
            setTimeout(() => { toast.style.display = "none"; }, 3000);
        }
    });
</script>